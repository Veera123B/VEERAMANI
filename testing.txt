1. git --version --------------to find the git version in your local machine 
git version 2.50.1.windows.1    ---------- This is the result



2. git congif --global user.name "veeramani balasundaram"   -----------------for configuration in my local machine
   git config --global user.email"veeramani2306@gmmail.com" ------------------ for configuration in my local machine 

3. git config --list  -----------to verify the login and username 

we can create the folder by manually or we can creating using mkdir (foldername) in cmd 
after folder created then we have to init the folder 


4. git init ---------this the initiaion 

5.echo hello Github > README.md ------------ this is used for create the file for project discription 

6. git status ---------------if any untracked file will be there it will show 

7. git add README.md ------------ to stage the file 

8.git commit -m "content" ----------------this is the commit every time we have to run this query

9.git add (file_name) -----------------the update file added in git 

10. git commit -m "content" --------------again we have to commiy

11.git remote add origin "repositaoryname"  ------------this is integrating with github location 

12. git push origin -u main -----------------this is used for last file update to the github location


